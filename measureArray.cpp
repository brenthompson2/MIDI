// Brendan Thompson
// measureArray.cpp

// creates the measureArray object

public measureArray(){
	BEAT measureArray[16];
}

public measureArray.addEvent(int subIndex, EVENT newEvent){
	BEAT current;
	current = measureArray[subIndex];
	int newIndex;
	newIndex = current.numEvents + 1;
}